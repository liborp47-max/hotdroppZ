# HDUA environment — copy to .env.local and fill in.
# Public values are safe to ship in the client bundle (EXPO_PUBLIC_ prefix).

# Supabase (same project as HDCC; anon/publishable key only — never the service role)
EXPO_PUBLIC_SUPABASE_URL=https://cudycxvbpewmuhxydcas.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Content API base (HDUA-02). For local dev point at the API host.
EXPO_PUBLIC_CONTENT_API_URL=http://localhost:3001/v1
